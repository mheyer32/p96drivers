name: P96 Drivers CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    # Pin explicitly — :latest is gcc-v6.5.0b; also available: gcc-v6.5.0b, gcc-v13.4, gcc-v15.2, gcc-v16.1
    container: stefanreinauer/amiga-gcc:gcc-v13.4

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Mark git directory as safe
        run: git config --global --add safe.directory '*'

      - name: Build all driver targets
        run: make all

      - name: Archive CI artifacts
        uses: actions/upload-artifact@v4
        with:
          name: p96drivers-build-artifacts
          path: |
            _bin/*
          if-no-files-found: error
